{% extends "base.html.twig" %}

{% block body %}
 {{ include('seller/nav.twig') }}

{# <pre>
    {{ dump(model) }}
</pre> #}

<h2>Model Detals</h2>
<table>
    <tr>
        <td>name</td>
        <td>description</td>
        <td>photo</td>
        <td>price</td>
    </tr>
    <tr>
        <td>{{model.product.name}}</td>
        <td>{{model.product.description}}</td>
        <td><img src="{{ asset('uploads/photo/' ~ model.product.photo) }}" height="100px"/></td>
        <td>{{model.product.price}}</td>
    </tr>

</table>

    {{form_start(form)}}
    {{form_widget(form)}}
    {{form_end(form)}}
{% endblock %}